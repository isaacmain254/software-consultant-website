import React from "react";

const NotFound = () => {
  return (
    <div
      className="not-found"
      style={{
        dislay: "flex",
        alignItmes: "center",
        jusstifyContent: "center",
        width: "100%",
      }}
    >
      <h2>Page Not Found</h2>
    </div>
  );
};

export default NotFound;
